#!/usr/bin/env bash
set -e
[ -n "$NODENV_DEBUG" ] && set -x

echo -n "$(which nodenv-update) && "
echo -n 'echo -e "\033[1;32mreloading nodenv\033[0m" && '
echo -n 'eval "$(nodenv init -)" && '
echo -n 'echo -e " \033[1;32m|\033[0m  done"'

echo
